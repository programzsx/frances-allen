# Claude-Code
$$$$
我有一个git项目。只有一个`main分支`。我要修改`auth模块`。我要修改`payment模块`。
我第一步要使用`git worktree add ../auth-task -b trace-auth`和`git worktree add ../auth-payment -b trace-payment`。这会`创建branch`。这会有新的目录。这会创建worktree。这不是简单的创建branch。
----
然后我切换到两个对应的目录当中。我启动`claude code`。我就可以独立修改了。我修改完成了之后。我提交。然后我在main分支这里。我进行`git merge`。
----
然后我可以删除`worktree`。我可以删除`branch`。
这就是___的使用场景。
%%%%
worktree
$$$$
`/batch`实际上。我理解就是___和___的结合。是面向简单的批量的任务的。
%%%%
worktree
$$$$
不管是`subagents`还是`agent teams`还是其他的。`worker`就是claude的___。
%%%%
session
$$$$
如果你想要让`claude`使用一个不一样的tool。那么你就使用___暴露给claude。
%%%%
mcp
$$$$
For subagents in the current session, ___ opens a panel with `a Running tab` listing live subagents and `a Library tab` where you create and edit custom subagents.
----
对于当前session里的subagent，这个命令会打开一个面板： 
`Running标签页`：显示正在运行的subagent
`Library标签页`：用于创建和编辑自定义subagent
%%%%
/agents
$$$$
%%%%
$$$$
%%%%

